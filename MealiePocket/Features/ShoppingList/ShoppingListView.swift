import SwiftUI

struct ShoppingListView: View {
    var body: some View {
        NavigationStack {
            Text("Shopping List")
                .navigationTitle("Shopping List")
        }
    }
}
